package bham.student.txm683.heartbreaker.map.roomGraph;

import bham.student.txm683.heartbreaker.utils.graph.Node;

public class RoomNode extends Node<Integer> {

    public RoomNode(int id){
        super(id);
    }
}
